/carlos
*.test
*.out
*_report.txt
*.log
coverage.*
.DS_Store
.env
.env.local
*.swp
*~
/tmp/
/.carlos/
/dist/
/build/
/bin/

# Local-only Go workspace files.
go.work
go.work.sum

# Claude Code's local working area (worktrees, transcripts, settings).
/.claude/

# Planning / agent-onboarding documents stay out of the repo. CLAUDE.md
# and AGENTS.md are recognised by Claude Code / Codex / Cursor as local
# context pointers; we use them locally but they describe our internal
# process, not the shipped software. SPEC.md (when present) is a
# planning artefact — the canonical spec lives in the private vault.
/CLAUDE.md
/AGENTS.md
/SPEC.md
