# branding (repo)

Canonical assets that the carlos binary embeds via `go:embed`. Source files and decision notes live in the vault (`personal/projects/carlos/branding/`); this folder only holds what ships.

Expected contents:

- `carlos-portrait.png` — source portrait, embedded for TUI onboarding
- `carlos-portrait-ascii.txt` — pre-baked ASCII fallback (24 lines)
- `cap-mark-{16,32,64,128,256}.png` — favicon set for future website
